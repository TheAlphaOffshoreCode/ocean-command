# ADR 0004: Event-driven evolution

Status: accepted. Domain changes produce versionable events; audit records are the first implementation. A broker is introduced only when asynchronous load requires it.
