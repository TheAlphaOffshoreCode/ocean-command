# Event catalog

Identity events are `organization.created`, `auth.login`, `auth.logout`, and `user.created`. They are currently audit events. Future domain events will include an explicit version, organization ID, entity identity, occurrence time, source, correlation ID and payload.
