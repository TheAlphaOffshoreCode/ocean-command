# Contributing

Use Conventional Commits. Before a pull request run `make lint`, `make typecheck`, `make test`, and `make build`. Keep tenant isolation and audit behavior covered by tests; never add real operational or personal data to fixtures.
